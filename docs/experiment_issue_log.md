# Experiment Issue Log

Last updated: 2026-09-04 UTC

This file tracks cross-session issues that affect experiment interpretation or
future decisions. Detailed per-session notes live in `memory/sessions/`.

| ID | Status | Area | Issue | Current decision |
| --- | --- | --- | --- | --- |
| I001 | open | E-DAIC | Transcript CSVs do not expose a speaker column. | Speaker-specific interviewer/participant controls require separate transcript/protocol work. |
| I002 | open | CMDC | `SubjectInfo.xlsx` duplicates `MDD20` and omits folder `MDD21`. | Treat as metadata quality risk; do not call the upload partial. |
| I003 | open | PDCH | Subject `034A` has two audio/text segments but no HAMD annotation. | Keep rows marked `missing_label`; exclude from supervised HAMD training/evaluation. |
| I004 | closed | MODMA | Five WAV files are invalid: `02010004/24.wav` through `02010004/28.wav`. | Governed in manifest/audit as `file_valid=false` with `invalid_audio:LibsndfileError`; exclude from supervised diagnostics and keep 1503 valid MODMA rows. |
| I005 | closed | MPDD | Server initially lacked MPDD OpenFace features. | Features synchronized and Phase 2 OpenFace baseline completed. |
| I006 | open | MPDD | Official materials document test label CSVs, but local copy still lacks them. | Existing train-only OOF results remain valid; test-only protocol requires local sync and no test-label tuning. |
| I007 | closed | MPDD/P3HF | P3HF was a Phase 2 conditional public baseline. | Marked `not_applicable` for canonical matrix because split/features/evaluation contract does not match. |
| I008 | open | CMDC/RQ2 | CMDC audio/video-only baselines are suspiciously high, and Phase 3 text question-position probes show large per-question performance variation. | Treat as confirmed protocol/task-content shortcut risk; later methods must report or control question-position dependence. |
| I009 | open | PDCH public baseline | Qwen2-Audio public reproduction parses all 17 HAMD factors for only 46/99 subjects. | Treat as weak public baseline and cite parsing caveat. |
| I010 | open | RQ2/Phase3 | Dataset identity is nearly perfectly recoverable from frozen/lightweight representations: WavLM six-way balanced accuracy 0.990, CMDC/PDCH BGE text 1.000, E-DAIC/CMDC OpenFace 1.000. | Direct pooled training cannot be interpreted as shared depression representation unless later methods control, penalize, stratify, or report dataset/protocol identity effects. |
| I011 | open | E-DAIC/RQ2 | Phase 3 protocol controls cannot run literal participant-only/interviewer-only tests because speaker labels are unavailable, but front-25% and repeated-turn-only proxy controls outperform full-dialogue binary TF-IDF. | Treat E-DAIC protocol/fixed-turn shortcut risk as supported, while keeping literal speaker-resolved controls blocked until a speaker-labeled transcript source is found. |
| I012 | open | MODMA/RQ2 | Phase 3 task-transfer diagnostics show moderate cross-task degradation, strongest for affective-task evaluation: Balanced Accuracy drop 0.142 with CI 0.003 to 0.280. | Treat task dependence as supported mainly for affective-task stress; later methods should report task-stratified performance and avoid claiming task-invariant speech evidence without controls. |
| I013 | open | EATD/RQ2 | Phase 3 audio eGeMAPS valence diagnostics do not show healthy negative material masquerading as depression: healthy negative predicted-depressed rate is 0.118 versus 0.206 for nonnegative material. | Keep as weak/negative evidence; do not add a valence-adversarial component solely from this eGeMAPS diagnostic, but revisit text/semantic valence if needed. |
| I014 | open | MPDD/RQ3 | Structured `gender` and `health_condition` fields are empty in the MPDD manifest. | Gender-only, health-only, and gender/health subgroup calibration diagnostics are blocked until structured metadata is supplied. |
| I015 | open | MPDD/RQ3 | Personality-only diagnostics beat shuffled personality, but generic audio-video-personality early fusion adds near-zero value over audio-video and shuffled personality inside AVP does not hurt performance. | Continue personality shortcut/moderation and calibration audits; do not treat naive AVP concatenation as a supported method component. |
| I016 | open | MPDD/RQ3 | Gait statistics show modest psychomotor context association with PHQ-9, top absolute Spearman 0.269. | Use gait as context-validation evidence, not as an automatically fused fourth modality. |
| I017 | closed | CMDC/RQ1 | Earlier Phase 4 label-contract audit overcounted CMDC HAMD item coverage by treating placeholder NaN payloads as item labels. | Corrected audit counts only valid numeric item payloads: CMDC has 25/78 subjects with aligned HAMD total+full-item labels. Use CMDC HAMD only as a small sanity subset; PDCH remains the primary HAMD MV02 source. |
| I018 | open | RQ1/Label contract | EATD, MODMA, and MPDD currently expose only total/severity labels for SDS or PHQ-9, not item-level labels. | Use them for total/severity external validation, valence/task/context stress tests, and calibration checks; do not claim item-level construct supervision from these datasets unless item labels are recovered. |
| I019 | open | Phase5/P5_MV01 | First runnable PHQ-8/PHQ-9 construct bridge over frozen WavLM is weak/asymmetric, and E-DAIC vs CMDC dataset identity balanced accuracy is 1.000. | Treat P5_MV01 as a diagnostic baseline, not evidence of a shared symptom representation; continue with identity/protocol-control validation or a revised feature contract before richer methods. |
| I020 | closed | Version management | Current working tree tracks zero Phase 2 baseline result artifacts, but local Git history still contains early `analysis/phase2_baselines/` artifact commits. | First GitHub upload completed through clean remote `main` root commit `a67cfdb`, created from a safe Git archive snapshot. Do not push the old local `main` history directly; future remote updates should use `scripts/publish_clean_github_snapshot.py` or another verified clean publish path. |
| I021 | open | Phase5/P5_MV04 | Train-fold dataset centering reduces E-DAIC/CMDC frozen-WavLM identity probes while preserving PHQ C01-C08 Macro MAE, but it uses known eval dataset labels for centering. | Treat as a successful diagnostic identity-control ablation, not a deployable unknown-source inference contract; extend to inference-compatible controls or protocol/task slices before full-method claims. |
| I022 | open | Phase5/P5_MV04b | Source-agnostic nuisance projection reduces prediction identity without eval dataset labels, but feature identity remains high: best feature BA `0.925`, prediction BA `0.777`. | Treat as a partial diagnostic control; do not claim dataset-invariant shared representation from frozen WavLM, and continue with stronger controls, task/protocol slices, or another minimal-validation row. |
| I023 | open | Phase5/P5_MV03 | EATD SDS total external stress is runnable, but current frozen audio features do not beat train mean: best all-valence MAE `7.341` versus train-mean `7.201`. | Treat as weak/negative SDS external stress evidence; do not claim SDS item-level or positive SDS total generalization without an audited stronger feature contract. |
| I024 | open | Phase5/P5_MV05 | MPDD AV-probability-first context calibration is runnable, but the proposed context calibrator worsens required age/personality subgroup ECE gaps and is weaker than AV-probability-only recalibration on key checks. | Treat as negative RQ3 context-calibration evidence; do not claim personality/age context conditioning until a revised design improves subgroup gaps against AV-only recalibration and shuffled controls. |
| I025 | open | PDCH/RQ1 | Seven PDCH HAMD-labeled subjects contain item code `9`; raw item sums are `+9` above manifest totals. | Official PDCH evaluation treats `9` as not sure/not applicable and excludes it from total scoring; scored item sums match manifest totals for 99/99 labeled subjects. Use manifest HAMD total as primary severity target and apply official `9 -> 0 for total` scoring when deriving totals from item heads. |
| I026 | open | E-DAIC/RQ1 | E-DAIC PHQ-8 totals cover 275 subjects, but valid item payloads cover 219 subjects in the current manifest. | Item-level PHQ construct work should use the 219 item-labeled train/dev subjects only; do not use official test total-only labels for item-level supervision or tuning. |
| I027 | open | Phase5/P5_MV02 | PDCH-only HAMD auxiliary bridge passes as an internal diagnostic, but CMDC 25-subject sanity does not support transfer: train-mean total MAE `3.595` beats feature heads, and eGeMAPS/early fusion degrade badly. | Use MV02 as bounded PDCH evidence only. Do not claim cross-dataset HAMD generalization from the current frozen-feature contract; require stronger controls or a revised feature/measurement design before full-method HAMD claims. |
| I028 | open | Phase5/P5_MV06/RQ4 | Evidence localization is ready to start from local MV01/MV02 predictions, but verbatim excerpts, source paths, and per-subject rationales are privacy-sensitive. | Keep MV06 raw-text review and candidate queues local-only. Commit only aggregate evidence agreement, prompt-artifact rates, evidence-source distribution, and construct coverage after artifact hygiene checks. |
| I029 | closed | Phase5/P5_MV06/RQ4 | The MV06 annotation pilot, two-annotator local workbench, AI preannotation triage, human review pack, and aggregate summary gate were ready, but initially no human/double annotation or agreement audit had been completed. | Resolved by importing human workbooks and rerunning `scripts/phase5_summarize_mv06_evidence_annotations.py`: latest aggregate status is 143 completed candidates, 143 double-annotated candidates, `ready_for_aggregate_evidence_review`, artifact hygiene passed. |
| I030 | open | Phase5/P5_MV03b | EATD text-semantic SDS total stress is runnable, but character TF-IDF heads improve validation MAE over train mean by only `0.00056`, below the meaningful-improvement threshold. | Treat as weak/negative SDS text evidence. Do not claim EATD SDS total or text-semantic cross-scale generalization without a stronger separately audited feature contract. |
| I031 | open | Phase5/P5_MV02b | PDCH manifest-text HAMD measurement audit is runnable, but fixed character hashing heads improve item-derived total MAE by only `0.008` and macro item MAE by only `0.0002` versus train mean. | Treat as weak/negative raw-text probe evidence. Keep MV02 frozen BGE/early-fusion as the stronger PDCH diagnostic signal; do not claim positive HAMD semantic measurement from lightweight text hashing. |
| I032 | open | Phase5/P5_MV04c | Protocol/task/valence nuisance projection gives mixed evidence: MODMA task projection reduces feature task-identity BA `0.762 -> 0.570` while preserving pooled Balanced Accuracy `0.688 -> 0.686`, but EATD SDS Ridge remains below train mean (`28.810` vs `7.201` MAE) and valence identity is not reduced. | Treat MODMA as a useful diagnostic task-control pass, but keep EATD as negative/blocked evidence. Do not start a full protocol/valence-adversarial method from MV04c alone; require stronger cross-dataset/shared-symptom evidence or completed MV06 annotation evidence. |
| I033 | open | Phase5/full-method gate | Phase 5 full-method gate audit blocks full M0/M1/M2/M3 construction while evidence remains mixed, negative, or annotation-incomplete. | Use `scripts/phase5_full_method_gate_audit.py` as the authoritative claim boundary. Full method remains blocked; a bounded diagnostic/audit-driven paper direction is viable if claims are reframed and the negative evidence is reported. |
| I034 | closed | Phase5/P5_MV07 | Current cached features were insufficient for the preferred aligned-BGE shared-symptom row because E-DAIC BGE was missing. | Resolved locally by generating `analysis/phase2_baselines/edaic_text_bge/edaic_bge_subject_features.csv` for 219 E-DAIC train/dev item-labeled subjects; keep the feature CSV local-only. |
| I035 | closed | Phase5/P5_MV07 | The aligned BGE feature contract was ready for E-DAIC/CMDC/PDCH, but no MV07 shared-symptom validation result existed yet. | Resolved by running `P5_MV07 aligned_bge_shared_symptom_validation`; tracked summaries and hygiene passed. |
| I036 | open | Phase5/P5_MV07 | Aligned-BGE MV07 is runnable but blocked as shared-symptom evidence: pooled PHQ itemwise Ridge does not consistently beat total-allocation floors and identity remains high. | Treat MV07 as negative/blocked evidence, not a shared-representation claim. Next options are completing MV06 annotations for evidence credibility or designing a stronger shared-symptom feature/identity-control contract. |
| I037 | open | Phase5/P5_MV07b | BGE train-fold nuisance projection reduces identity substantially but still fails the CMDC total-allocation floor: best k=10 feature identity BA `0.709`, prediction identity BA `0.684`, three-way feature identity BA `0.687`, CMDC delta vs total allocation `+0.018`. | Treat MV07b as partial diagnostic evidence. It shows identity can be reduced without eval dataset labels, but it does not authorize a transferable shared-symptom representation or full-method start until the identity-controlled floor gap is resolved or the paper is reframed around bounded diagnostics. |
| I038 | open | Phase5/P5_MV07c | Train-fold-selected total anchoring over identity-projected BGE still fails the shared-symptom floor gate: prediction identity BA `0.664`, E-DAIC delta vs raw total allocation `-0.018`, but CMDC delta vs raw total allocation `+0.012` and vs projected total allocation `+0.002`. | Treat MV07c as a negative follow-up that closes the obvious "just total-anchor itemwise heads" explanation. Stop iterating small shallow BGE-head variants unless the feature or measurement contract changes; prioritize MV06 human annotation or a reframed diagnostic paper direction. |
| I039 | closed | Phase5/P5_MV06/RQ4 | MV06 first-round agreement was dataset-stratified, but E-DAIC initially had only 2 double-annotated pairs and degenerate evidence-presence kappa marginals. | Resolved by importing the updated workbook: E-DAIC now has 24 evidence-presence double-annotation pairs with kappa `0.846`; overall/CMDC/PDCH evidence-presence kappas are `0.965`/`0.967`/`1.000`. |
| I040 | open | Version management/data governance | Public Git history currently contains earlier real row-level subject manifests, file-integrity rows, and subject split maps with labels, subject IDs, or local path columns. | Latest-tree mitigation is complete: real row-level dataset tables are untracked/ignored while schemas, synthetic examples, and aggregate audits remain public. Remote history rewrite or repository recreation remains optional and must be explicitly approved before force-pushing. |
| I041 | open | RQ1/method target | Phase 5 shallow fixed-map shared-symptom rows do not support a transferable shared symptom representation; BGE identity controls reduce shortcut signal but fail total-allocation floors. | Reframe the method target as partial measurement invariance, but the first MV08 lightweight ordinal pilot is also negative. Treat this as a measurement-model problem, not a reason to return to small BGE head variants. |
| I042 | closed | Phase5/P5_MV08/RQ1 | The MV08 partial-invariance measurement design audit was ready, but no MV08 trainer or pilot result existed yet. | Resolved by implementing and running `scripts/phase5_run_mv08_partial_invariance_measurement.py`; tracked aggregate outputs and hygiene passed. The result is negative and is tracked separately in I043. |
| I043 | closed | Phase5/P5_MV08/RQ1 | The first MV08 partial-invariance ordinal measurement pilot is blocked as positive RQ1 evidence: M2 improves over the total-score floor on `0/3` pooled active slices, with worst pooled deltas `+0.152` macro item MAE versus total-score floor and `+0.140` versus fixed map; feature identity BA is `1.000` and M2 prediction identity BA is `0.900`. | Resolved by running aggregate MV08 error analysis. It confirms current MV08 is not claimable positive RQ1 evidence: pooled M2 is worse than total/fixed floors on all active slices, largest pooled item delta is CMDC PHQ9_8/C08 (`+0.698` MAE versus total floor), and HAMD DIF heads have threshold sparsity (`0.318` constant-threshold fraction). |
| I044 | closed | Phase5/P5_MV08b/RQ1 | MV08 error analysis recommended either freezing the current MV08 contract as negative evidence or predeclaring a genuinely changed MV08b measurement mechanism. | Resolved by `scripts/phase5_plan_mv08b_total_anchored_residual_measurement.py`: MV08b is predeclared as a total-anchored residual item-measurement design with pooled/collapsed threshold policy, HAMD as separate clinical stress test, and total-score/fixed-map/identity gates. |
| I045 | closed | Phase5/P5_MV08b/RQ1 | MV08b was design-ready but had not been implemented or run. | Resolved by implementing and running `scripts/phase5_run_mv08b_total_anchored_residual_measurement.py`. Artifact hygiene passed and subject overlap was zero, but MV08b failed the predeclared identity gate: M2b beats both total-score and fixed-map floors on 2/3 pooled active slices, while prediction identity BA rises to `0.979` versus the MV08 M2 gate `0.900`. |
| I046 | open | Phase5/RQ1/paper framing | MV08/MV08b/MV12/MV15/MV16 form bounded negative or diagnostic RQ1 evidence; MV17a is now the canonical multilingual feature-contract consequence layer. | Freeze this modeling sequence and stop small shallow RQ1 head iteration unless a genuinely new data, feature, or measurement source is introduced. After MV14 convergence-safe uncertainty, MV19 finite-sample downgrade, MV17a multilingual sensitivity, MV15/MV16 legacy negative controls, and MV06 agreement uncertainty, frame the paper as measurement shift / measurement validity: substantial common PHQ structure, repeated but finite-sample-bounded `C02/C06` threshold-shift evidence, uncertain global invariance-model selection, high feature identity after theta/severity conditioning, bounded/negative DIF-guided few-shot calibration, and first-round aggregate evidence-localization credibility. MV17a does not support universal external-theta-transfer failure or universal B3 dominance; both are encoder-dependent. |
| I047 | open | Phase5/RQ2/RQ1 identity gate | Unconditional dataset identity BA can be confounded by legitimate severity, scale, language, protocol, and population differences. | MV09 completed a conditional identity audit and revised future gate semantics: unconditional identity is a shortcut-risk screen; shared-latent claims must report identity after conditioning on available severity, aligned items, and legitimate covariates; scale-specific post-head prediction identity is diagnostic rather than the same hard gate. MV15 strengthens this blocker under dimension-matched controls: E-DAIC/CMDC raw, total-conditioned, predicted-total-conditioned, B3-conditioned, and theta-conditioned BGE feature identity BA are all `1.000`; PHQ-item-conditioned feature identity BA is `0.974`; theta-only identity BA is `0.576`. |
| I048 | closed | Phase5/RQ1/psychometrics | MV10 label-only PHQ invariance screen supports substantial common PHQ structure, but threshold/scalar agreement is not uniform. | Resolved as an approximate measurement-shift baseline and carried forward into MV11/MV13/MV14: configural screen passes, loading congruence is `0.998`, metric-loading items are `7/8`, threshold/scalar items are `4/8`, and candidate anchors are `C01`, `C04`, `C05`, `C07`. Full method remains blocked. |
| I049 | open | Phase5/RQ1/formal psychometrics | MV11 formal label-only graded-response IRT confirmation preserves the MV10 anchor map but leaves an AIC/BIC caveat. | Use MV11 as bounded formal in-repo measurement evidence; corrected MV13/MV14 provide anchor-linked external mirt qualitative/uncertainty corroboration. The broader result supports item-level measurement-shift framing, not a positive multimodal method claim, final global invariance-model selection, or robust standalone mirt DIF evidence. |
| I050 | closed | Phase5/RQ1/MV12 latent target | MV12 predeclared the two-stage latent-target experiment, but initially had not produced any multimodal `X -> theta` result. | Resolved by implementing and running `scripts/phase5_run_mv12_two_stage_latent_target.py`; tracked aggregate outputs and hygiene passed, while row predictions, theta scores, fitted parameters, transformed features, projection directions, and model artifacts remain local-only. |
| I051 | closed | Phase5/RQ1/MV12 latent target | Legacy MV12 is informative but blocked under the old Chinese-BGE chain: `X -> theta` improves same-dataset theta MAE versus train mean on E-DAIC and CMDC, and conditional predicted-theta identity BA is `0.602`, but observed-scale reconstruction is worse than direct itemwise Ridge and old-chain source-calibrated external theta transfer fails. | Resolved by `scripts/phase5_analyze_mv12_latent_target_tradeoffs.py`: aggregate tradeoff/failure-mode analysis recommends freezing the legacy latent-target line. The interpretation is downgraded: M12a is lower-identity than upstream BGE features, but B3 direct itemwise Ridge compressed to theta has lower pooled observed macro MAE (`0.692` vs `0.701`) and lower conditional identity (`0.579` vs `0.602`), so MV12 does not prove psychometric theta is uniquely more invariant. MV17a supersedes universal external-transfer-failure and universal B3-dominance wording. Full method remains blocked. |
| I052 | closed | Paper/MV12 measurement framing | The current MV12 line was frozen after aggregate tradeoff analysis, but manuscript sections had not yet converted Phase 2/3 and MV07-MV12 aggregate evidence into a coherent Baselines, Failure-Mode Diagnostics, and Measurement Results narrative. | Resolved by `scripts/build_diagnostic_paper_results_sections.py`, which generates aggregate-only Baselines, Failure-Mode Diagnostics, and Measurement Results scaffolds with source maps, claim checklist, run summary, and hygiene audit. Do not start full M0/M1/M2/M3 unless a genuinely new predeclared mechanism changes the full-method gate. |
| I053 | closed | Phase5/MV13 external psychometrics | MV11 uses an in-repo formal graded-response IRT implementation, so manuscript reviewers may ask for an independent psychometric replication. | Resolved by installing R/lavaan plus archived R-compatible `mirt` and running `P5_MV13_external_psychometric_replication`: external `mirt::multipleGroup` qualitatively replicates MV11 with 4 confirmed anchors, 0 loading-DIF flags, threshold DIF on `C02`/`C06`, AIC partial vs BIC scalar split, aggregate-only exports, and a retained configural convergence warning. |
| I054 | closed | Phase5/MV14 measurement uncertainty | CMDC PHQ item supervision has only 77 subjects, making threshold/DIF estimates unstable if reported from a single fit; the initial MV14 summary counted fit-success draws rather than convergence-safe draws for AIC/BIC and LRT. | Resolved for convergence-safe counting and later refreshed by the corrected anchor-linked focal hyperparameter rerun: AIC/BIC selection and LRT require `fit_success && converged`, item-DIF effective counts require `comparison_valid == TRUE`, and threshold-constrained models now free focal mean/variance through `free_means/free_var`. Smoke/core/DIF R=`10/200/100`; convergence-safe full-ladder effective core R is `120/200` after `185/200` fit-success draws; configural converges in `120/200`; stable metric/partial/scalar ladder effective R is `198`; minimum DIF anchor-support effective R is `77/100`, while threshold-DIF comparisons are `100/100` effective; stable anchors are `C01/C04/C05/C07`; top threshold-DIF items are `C02/C06`. MV14 is corrected but still convergence-aware and finite-sample-bounded, not bootstrap-confirmed robust standalone DIF. |
| I055 | closed | Phase5/MV15 latent-conditioned identity design | MV09 conditions identity on raw severity or item labels, but MV10/MV11/MV19 show those labels themselves have measurement shift; corrected MV13/MV14 keep that psychometric caveat active under anchor-linked mirt corroboration; MV12 also shows M12a is not better than a dimension-matched B3 severity baseline. | Resolved by `scripts/phase5_plan_mv15_latent_conditioned_identity.py --overwrite`: MV15 design now has 10 conditioning ladder rows, 7 identity probes, 8 pass/fail gates, and explicit total, predicted-total, observed-item, B3 itemwise-theta, psychometric-theta, covariate, predicted-output, and severity-only controls. Use local-only theta scores/residualized features/row predictions and aggregate exports only. |
| I056 | closed | Phase5/MV16 theta calibration | Legacy MV12 indicates `X -> theta` is learnable within source datasets but old-chain source-calibrated external theta transfer fails; MV14 localizes stable threshold non-equivalence to `C02/C06` with anchors `C01/C04/C05/C07`; MV16 was predeclared to test DIF-guided few-shot calibration. | Resolved by implementing and running `scripts/phase5_run_mv16_dif_guided_calibration.py`. Aggregate outputs and hygiene passed; subject overlap was zero. MV16 status is `blocked_no_dif_guided_small_k_gain`: split, ladder, anchor-safety, direct-baseline, output-identity, and artifact-hygiene gates pass, but the both-direction DIF-guided small-k mechanism gate fails. Best supported row is `D1_edaic_source_cmdc_target`/`M16d_global_plus_C02_C06` at k=`10`; best L4 small-k delta theta MAE vs L0 is `-0.227`, and L4 small-k output identity BA is `0.984`. Treat as bounded/negative calibration evidence; MV17a now owns cross-encoder prediction-consequence wording. |
| I057 | open | Phase5/P5_MV06/RQ4 | The updated MV06 workbook import strengthens first-round agreement and now includes aggregate nonparametric bootstrap agreement uncertainty, but the attachment omitted one CMDC candidate's two annotator rows. | Use MV06 as aggregate first-round credibility evidence. Evidence-presence kappa 95 percent CIs are now reported in `agreement_uncertainty_summary.csv` and paper scaffolds. Before stronger RQ4 wording, resolve the remaining incomplete local candidate if available and discuss sampling limits while keeping workbooks, snippets, notes, source locators, and subject-level rows local-only. |
| I058 | closed | Phase5/MV15 runner | MV15 was design-ready but had not produced identity-probe results yet. | Resolved by implementing and running `scripts/phase5_run_mv15_latent_conditioned_identity.py`. Aggregate outputs and hygiene passed; subject overlap was zero. Result is `blocked_theta_conditioned_feature_identity_high`: raw/theta/total/predicted-total/B3-conditioned BGE feature identity BA is `1.000`, PHQ-item-conditioned feature identity BA is `0.974`, theta-only identity BA is `0.576`, predicted-theta output identity BA is `0.646`, and B3 output Pareto-dominates predicted theta in the legacy old-chain run. Freeze this line as negative diagnostic evidence; MV17a supersedes universal B3-dominance wording. |
| I059 | open | Phase5/MV14 scale-up sensitivity | MV14 convergence-safe logic and anchor-linked focal hyperparameter handling are fixed at core R=`200`, but manuscript-level intervals may benefit from a larger corrected bootstrap run. | Optional only if reviewer-facing interval precision becomes necessary: rerun MV14 with the same corrected anchor-linked focal mean/variance contract and larger core/DIF tiers such as 500-1000. Do not open this as a new experiment queue; it is a precision sensitivity only. |
| I060 | closed | Phase5/MV16 DIF-guided calibration design | MV16 had only an issue-level sketch after MV15; no implementation-ready design contract existed. | Resolved by `scripts/phase5_plan_mv16_dif_guided_calibration.py --overwrite`: MV16 design now has E-DAIC->CMDC and CMDC->E-DAIC PHQ directions, k=`0/5/10/20/40`, anchors `C01/C04/C05/C07`, primary `C02/C06` threshold-DIF calibration, global affine/monotonic, all-threshold, zero-shot, direct-adaptation comparators, eight gates, and explicit local-only boundaries. The subsequent MV16 run is tracked in I056. |
| I061 | closed | Paper/MV16 consolidation | MV16 is complete but did not pass the small-k DIF-guided mechanism gate. | Resolved by generating aggregate-only manuscript draft v0.1 with MV16 framed as bounded/negative calibration evidence and MV06 agreement uncertainty included as first-round evidence-localization credibility. Do not use it to authorize full M0/M1/M2/M3 or feature-invariance wording. |
| I062 | open | Paper/manuscript editing | Manuscript draft v0.1 is generated from aggregate artifacts with traceability, open editing items, artifact-hygiene checks, and a first formal bibliography handoff. | Use `analysis/diagnostic_measurement_audit_paper/manuscript_draft.md`, `references.bib`, `citation_registry.csv`, `citation_source_map.csv`, `manuscript_traceability_matrix.csv`, and `manuscript_open_items.csv` for human editing, citation-key insertion, venue-style reference formatting, and cross-reference cleanup. Keep claims within the full-method gate; stronger RQ4 wording still needs the remaining local MV06 candidate resolved or explicitly bounded. |
| I063 | closed | Phase5/BGE feature contract | The paper-critical MV07 -> MV12 -> MV15 -> MV16 BGE chain had a feature-contract caveat: E-DAIC MV07 generation used `BAAI/bge-small-zh-v1.5`, a Chinese BGE model, on English E-DAIC transcripts, and the available transcript CSV contract does not expose speaker roles for participant/interviewer filtering. | Resolved for the paper-critical chain by MV17a multilingual sensitivity. BGE-M3 and multilingual-E5 subject features were generated for E-DAIC/CMDC/PDCH and MV07/MV12/MV15 were rerun for both encoders. Both encoders reproduce the blocked pattern: MV07 `blocked_not_better_than_total_allocation_bge_contract`, MV12 `blocked_theta_gain_not_observed_scale_safe`, and MV15 `blocked_theta_conditioned_feature_identity_high`; both pass same-dataset theta utility, fail observed-scale validity, and keep theta-conditioned feature identity BA at `1.000`. External theta transfer is encoder-dependent (BGE-M3 passes, multilingual-E5 fails), and B3 Pareto dominance is encoder-dependent (false for BGE-M3, true for multilingual-E5). The old Chinese-BGE outputs remain legacy/diagnostic; this BGE caveat does not affect MV10/MV11/MV19 primary label-only psychometrics or corrected MV13/MV14 mirt corroboration. MV16 remains paused unless explicitly needed. |
| I064 | open | Paper/bibliography | The bibliography generator contained source-mismatch metadata for P3HF, Multi-Probe Audit, EMNLP interviewer-bias sources, and later needed the final SCD-MLLM article metadata. | The generator now uses corrected primary-source metadata for P3HF, Multi-Probe Audit, EMNLP interviewer bias, and SCD-MLLM as `Pattern Recognition` 177:113367 with DOI `10.1016/j.patcog.2026.113367`. Full DOI/publisher/ACL/arXiv verification of every bibliography row is still required before submission. Do not trust automatically generated references without primary-source checks. |
| I065 | open | Paper/framing | The strongest publishable contribution is target measurement validity, not a generic multi-dataset benchmark audit, a transferable shared-symptom model, a personality-aware model, or an evidence-retrieval method. | Reframe the manuscript around three layers: representation/protocol shift, target measurement shift, and prediction shift. Phase 3 becomes motivating evidence; MPDD/RQ3 becomes a population stress test; MV06/RQ4 becomes credibility support. |
| I066 | closed | Phase5/next experiments | The post-review route needed bounded experiments and manuscript/citation consolidation while several tempting model iterations stayed stopped. | Resolved through MV17a, MV18, MV19, and MV20. The consolidation inventory defines the active evidence bundle and removes early weak/superseded MV rows from the active queue. MV20 is a bounded negative CMDC-only criterion-overlap stress test: high-overlap deletion is not clearly worse than matched random deletion under BGE-M3 primary or multilingual-E5 sensitivity, so experiments are frozen and the next work is manuscript finalization plus primary-source citation verification. Stop extra shallow BGE heads, projection dimensions, overlap-threshold tuning, contamination-aware architectures, DIF-guided calibration variants, personality gating/calibrators, and EATD valence-adversarial modules unless a genuinely new design contract changes the gate. |
| I071 | closed | Phase5/MV20 criterion overlap | The paper needed one final bounded check that protocol/question text semantically close to PHQ/HAMD criteria was not driving the current prediction story. | Resolved by `scripts/phase5_run_mv20_criterion_overlap_stress.py`. CMDC was included because Q1-Q12 question-position units are available; PDCH and E-DAIC were excluded because clean question/prompt units were not available. BGE-M3 primary CMDC PHQ-9 top-20 all/minus-high/minus-random/high-only MAE is `3.571`/`3.918`/`3.768`/`4.215`; criterion excess loss versus matched random is `0.150` with 95 percent CI `-0.320` to `0.671`. Multilingual-E5 sensitivity also has `no_excess_criterion_overlap_evidence`. Treat as a completed negative stress test and stop further threshold tuning or contamination-aware model work. |
| I067 | open | Phase5/MV18 HAMD same-scale control | Same-language/same-HAMD CMDC versus PDCH control still shows dataset/context sensitivity but CMDC HAMD supervision is small. | Treat MV18 as exploratory context-shift support, not formal HAMD invariance: 25 CMDC HAMD subjects and 73 PDCH mild/moderate overlap subjects yield 4 predeclared severity-conditioned residual item-shift flags (`HAMD08`, `HAMD11`, `HAMD04`, `HAMD09`), 7 threshold-shift flags, and weak primary bidirectional transfer. Full-method HAMD generalization remains unsupported. |
| I068 | closed | Phase5/MV19 finite-sample PHQ simulation | The E-DAIC/CMDC PHQ item-labeled N is small, so MV10/MV11/MV13/MV14 C02/C06 and anchor wording needed observed-N simulation support. | Resolved by `scripts/phase5_run_mv19_phq_finite_sample_simulation.py` with 500 simulations per world. Status `complete_mv19_high_false_localization_downgrade_c02_c06`: H0 C02/C06 both-flag false rate `0.208`, H0 top-two false-localization `0.034`, H1 C02/C06 both-flag recovery `0.662`, H1 top-two recovery `0.222`, and H1 anchor subset recovery `0.178`. Downgrade C02/C06 from robust standalone DIF to repeated but finite-sample-bounded dataset-group threshold-shift evidence. |
| I069 | closed | Phase5/experiment consolidation | Phase 5 accumulated many minimal-validation rows, design contracts, local workflows, and negative follow-ups, creating risk that obsolete rows would keep inviting new iterations or clutter the manuscript. | Resolved by `scripts/phase5_consolidate_experiment_inventory.py`: current consolidation has 49 evidence/route rows, 6 paper-core rows, 13 paper-support rows, 28 retired/frozen rows, 1 paper guardrail, and 1 planning route after adding MV22/MV23. Tracked aggregate outputs are retained for traceability; no tracked experiment deletion is allowed by default. Interpreter/notebook caches may be deleted without user input; local predictions, features, Phase 2 outputs, MV06 workbooks, raw datasets, and the local original-plan note require separate user approval before deletion. |
| I070 | closed | Paper/MV17a claim calibration | The manuscript and claim tables still risked overclaiming universal external theta transfer failure, universal B3 Pareto dominance, or old Chinese-BGE MV12/MV15 results as the canonical feature contract. | Resolved by recalibrating the paper/gate generators and docs around MV17a: BGE-M3 is primary, multilingual-E5 is sensitivity, MV12/MV15/MV16 old-chain outputs are legacy/supporting, external theta transfer and B3 dominance are encoder-dependent, MV18 is exploratory same-HAMD context-shift support, and MV19 is finite-sample calibration of the current PHQ screening/localization procedure rather than a formal IRT power simulation. |
| I072 | closed | Phase5/MV13-MV14 mirt parameterization | The initial code-level correctness audit found that MV13/MV14 correctly set E-DAIC as reference and CMDC as focal, manually constrained anchors through `CONSTRAINB`, and used graded `d1-d3` threshold/intercept constraints, but omitted `invariance` terms needed to free focal-group latent mean/variance under anchor linking. | Resolved by narrowly patching MV13/MV14 R runners to add anchor-linked `free_means/free_var` for threshold-constrained models, rerunning MV13 and MV14, and rerunning `scripts/phase5_audit_mirt_parameterization_contract.py`. Current audit status is `complete_mirt_parameterization_consistent` with `statistical_correctness_blocker=false`; MV13/MV14 may be used as corrected anchor-linked external mirt corroboration with configural convergence and MV19 finite-sample caveats. |
| I073 | closed | Phase5/MV22 foundation backbone | The manuscript needed a real foundation-model-era validation slice rather than only saying the framework could use stronger encoders. | Resolved by `scripts/phase5_run_mv22_foundation_backbone_validation.py`. Qwen3-Embedding-0.6B subject features were generated locally for E-DAIC/CMDC/PDCH, MV07/MV12/MV15 were rerun and remain blocked, and aggregate ERM/CORAL/MMD/DANN/IRM/GroupDRO-style baselines plus WavLM base-plus audio proxy coverage were reported. Qwen feature identity BA remains `1.000`, prediction identity BA is `0.978`, MV12 remains `blocked_theta_gain_not_observed_scale_safe`, and theta-conditioned feature identity remains `1.000`; Qwen measurement-aware MV12 references improve shared-item macro MAE over direct itemwise references in both PHQ transfer directions. Treat as a bounded foundation-backbone stress test, not full WavLM Large/video/end-to-end multimodal method completion. |
| I074 | closed | Phase5/MV23 foundation multimodal completion | After MV22, the remaining practical critique was that the foundation-era validation still lacked an explicit lightweight multimodal comparison under the same measurement-aware contract. | Resolved by `scripts/phase5_run_mv23_foundation_multimodal_completion.py`. MV23 evaluates 8 audio, video-proxy, text-audio, and text-audio-video views over E-DAIC/CMDC PHQ shared-item transfer, with 288 adapter aggregate rows and 48 measurement-aware proxy rows. It includes WavLM/wav2vec2 audio proxies, OpenFace video proxy, Qwen3/BGE-M3/multilingual-E5 fusion views, ERM/CORAL/MMD/DANN/IRM/GroupDRO-style baselines, and a lightweight measurement-aware latent-total proxy head. Artifact hygiene passes. Treat as lightweight multimodal foundation stress-test support, not WavLM Large, VideoMAE, or end-to-end multimodal success. |
| I075 | closed | Phase5/MV24 formal measurement-aware method | The paper needed the measurement-aware model to be a real, unique architecture and needed a clean main result table rather than a loose menu of possible heads/losses. | Resolved by `scripts/phase5_run_mv24_measurement_aware_ordinal_model.py`. MV24 fixes the core method as a frozen Qwen3+WavLM+OpenFace representation, trainable projector, shared eight-dimensional PHQ symptom layer, and corpus-specific cumulative-logit ordinal heads trained with source ordinal reconstruction plus target calibration reconstruction. MMD is now an auxiliary variant, not part of the core method definition. After the fair-ablation rerun in I081, treat MV24 as a calibrated target-contract test with a bounded, direction-dependent measurement-parameterization claim. Artifact hygiene passes and outputs are aggregate-only. |
| I076 | closed | Phase5/MV24 supervision-budget fairness | The earlier MV24 table could be attacked because measurement-aware rows and corpus-specific-head used target calibration labels, while ERM/CORAL/MMD/DANN/strongest foundation/latent-only did not. | Resolved by regenerating MV24 with `label_budget_contract.csv`, `zero_target_label_result_table.csv`, `target_calibrated_result_table.csv`, and same-budget-only `paired_significance.csv`. I081 supersedes the stronger old wording: even within the calibrated block, corpus-specific-head is a weak frozen-representation comparator and cannot identify the measurement-aware target pathway by itself. |
| I077 | closed | Phase5/MV25 provenance and controlled identity | DAIC-WOZ/E-DAIC overlap and raw corpus identity BA=`1.000` were easy to attack if written as independent-corpus evidence or as simple proof of representation heterogeneity. | Resolved by `scripts/phase5_run_mv25_provenance_controlled_identity.py`. DAIC-WOZ/E-DAIC is explicitly documented as a same-lineage PHQ-8 sanity control (`141` paired complete train/dev overlaps, all-item exact match `0.993`, mean absolute item difference `0.007`, local DAIC-WOZ extracted folders symlinked into E-DAIC). Controlled identity probes show E-DAIC/CMDC raw `1.000` drops near chance after length/severity controls, so that raw score is only a shortcut-risk screen; same-language E-DAIC lineage probes remain high after controls (`0.839` Qwen3 text, `0.897` WavLM audio), supporting corpus/protocol identity beyond simple English-vs-Chinese recognition. Artifact hygiene passes and outputs are aggregate-only. |
| I078 | closed | Phase5/MV26 depression-specific baselines | The paper needed a closer baseline stress test than generic ERM/CORAL/MMD/DANN: GNN-SDA-style semi-supervised graph domain adaptation and QuestMF-style question-wise ordinal fusion under the same E-DAIC<->CMDC target calibration budget. | Resolved by `scripts/phase5_run_mv26_depression_specific_baselines.py`. MV26 uses the official MV24 Qwen3+WavLM+OpenFace representation, the same PHQ shared items, two directions, five seeds, and target calibration labels for every row. QuestMF-style measurement-aware improves reconstruction-plus-calibration in both directions (`1.203 -> 1.159`, `1.133 -> 1.096`). GNN-SDA-style is direction-sensitive (`1.121 -> 1.066` in E-DAIC-to-CMDC, `1.339 -> 1.431` in CMDC-to-E-DAIC due to calibration trade-off). Treat MV26 as a close-baseline stress test, not a direct reproduction of external leaderboard numbers. Artifact hygiene passes and outputs are aggregate-only. |
| I079 | closed | Phase5/MV26 SCD-MLLM-style baseline | The remaining public close-baseline critique was whether a stronger heterogeneous multimodal/foundation fusion baseline would make the measurement-aware target pathway unnecessary. | Resolved by `scripts/phase5_run_mv26_scd_mllm_baseline.py` and merged into `analysis/phase5_minimal_validation/p5_mv26_depression_specific_baselines/` through the canonical MV26 wrapper. The combined public MV26 package adds SCD-MLLM-style heterogeneous multimodal adapter/fusion under the same official MV24 E-DAIC<->CMDC PHQ shared-item split, Qwen3+WavLM+OpenFace representation, five seeds, and target calibration label budget. SCD-MLLM-style measurement-aware improves reconstruction-plus-calibration in both directions (`1.485 -> 1.238`, `1.100 -> 1.084`). Artifact hygiene passes and outputs are aggregate-only. |
| I080 | closed_local_not_committed | Phase5/MV27 four-domain binary benchmark | The user requested DAIC-WOZ+CMDC+MODMA+EATD pairwise binary transfer, same-budget target-aware comparisons, and formal ablation, but the first MV27 version had fairness bugs: GNN target-aware bypassed the shared head and GNN/DIL direct-versus-aware rows did not share the same adaptation recipe. | Fixed locally in `scripts/phase5_run_mv27_four_domain_binary_benchmark.py` and rerun. The corrected version uses one frozen WavLM subject backbone, 12 source-to-target directions, five seeds, no target labels for Table A, and matched target-calibration budgets for Tables B/C. GNN target-aware now uses `shared_head + corpus_specific_residual`; GNN/DIL direct and target-aware rows share pseudo-label/diversity/teacher adaptation recipes where applicable. The corrected result does not pass the automatic submission gate: Table B BA improvements are 18/72 cells and Table C Full beats the strongest same-budget ablation in 3/12 directions. Keep MV27 uncommitted unless the user explicitly approves using it as negative/diagnostic binary stress-test evidence. |
| I081 | closed | Phase5/MV24 fair shared-layer calibrated ablation | The Table 3 calibrated comparison still could not identify the contribution of the measurement-aware target pathway because corpus-specific-head froze the source-trained symptom layer while measurement-aware adapted shared layers with target calibration labels. | Resolved by adding direct target fine-tuning, direct source+target multitask, shared ordinal head, and generic target MLP head baselines to `scripts/phase5_run_mv24_measurement_aware_ordinal_model.py`, rerunning MV24 with five seeds, and adding `fair_ablation_gate` to `run_summary.json`. The gate status is `not_passed_uniform_measurement_pathway_superiority`: only 2/8 fair shared-layer comparisons pass. Manuscript wording now says target calibration/shared-layer adaptation is the robust finding, while the corpus-specific ordinal pathway is competitive and direction-dependent rather than uniquely proven. |
| I082 | closed | Phase5/MV24 targeted item and DIF simulation | After I081, the remaining question was whether corpus-specific ordinal heads at least help the exact `C02/C06` items flagged by the measurement gate, and whether a fixed-latent world with planted threshold DIF supports the proposed response-process mechanism. | Resolved by extending `scripts/phase5_run_mv24_measurement_aware_ordinal_model.py` with targeted item-set metrics and adding `scripts/phase5_run_mv24_measurement_head_dif_simulation.py`. Real-data targeted analysis shows shared ordinal and measurement-aware corpus-specific ordinal heads are near tied on all shared PHQ items, anchors `C01/C04/C05/C07`, and `C02/C06` threshold-shift items (`0.004` and `0.002` C02/C06 deltas, intervals crossing zero). The fixed-latent simulation shows no benefit under scalar invariance and only weak item-local consistency under planted `C02/C06` threshold DIF (`0.002`/`0.011` C02/C06-set deltas; `301/500`/`311/500` lower-error draws). Keep corpus-specific heads as a constructive framework instantiation, not as an independently supported real-data performance source. |
| I083 | closed_bounded | Phase5/MV32 TCPS partial sharing | ICASSP-oriented novelty needed a method stronger than a fixed corpus-specific ordinal head, but without adding unrelated Mamba/LLM/fusion modules. | Resolved by implementing `scripts/phase5_run_mv32_tcps_partial_sharing.py` and running MV32 at `analysis/phase5_minimal_validation/p5_mv32_tcps_partial_sharing/`. TCPS learns sparse target-corpus threshold residuals between shared and fully corpus-specific ordinal heads, uses PCA fitted only on source plus target calibration subjects by default, reports lambda-path sensitivity, targeted item errors, and paired participant bootstrap deltas. The final gate is `borderline_audit_guided_algorithm_candidate`: real-data TCPS evidence is partial, participant-bootstrap superiority is not interval-stable, and simulation supports the mechanism only weakly/moderately. Use TCPS as an audit-guided algorithmic instantiation, not as stable real-data superiority over shared or fully corpus-specific ordinal heads. |
